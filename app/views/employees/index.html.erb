<% @employees.each do |employee| %>
  <h2><%= link_to employee.name, employee_path(employee) %></h2>
  <h4><%= employee.title %></h4>
  <p><%= employee.company.name %></p>

  <%= button_to "Remove", employee_path(employee), :method => :delete, :class => "employee-button"%>

<% end %>
