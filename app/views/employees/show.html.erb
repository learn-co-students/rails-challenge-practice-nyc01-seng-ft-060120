<h4>  <%= @employee.name %></h4>
  <%= link_to @employee.company.name, company_path(@employee.company) %> <br>
<p>  <%= link_to "Delete Employee", company_employee_path(@employee), method: :delete, data: { confirm: "Are you sure?" } %></p
