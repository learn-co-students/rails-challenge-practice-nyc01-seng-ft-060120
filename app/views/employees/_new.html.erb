<%= form_with(model: [@company, employee ], local: true) do |em| %>

<%= em.label :name %>
<%= em.text_field :name %>

<%= em.label :title %>
<%= em.text_field :title %>

<%= em.collection_select :company_id, Company.all, :id, :name %>

<%= em.submit %>

<% end %>